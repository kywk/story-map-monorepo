# 0.1.1 follow-up

Date: 2026-09-26. The owner requested committing the preparation and assistance with
manual npm setup. Preparation was committed as `6a57e67`.

Registry inspection then confirmed all three libraries already have `0.1.0` on `latest`,
published on 2026-09-25 by `kywk`, with repository metadata pointing at
`kywk/story-map-monorepo`. Their SHA-512 integrities differ from the prepared artifacts.
The owner explicitly selected a lockstep `0.1.1` release rather than overwriting `0.1.0`.

Implementation: bump only the three library manifests, make publication tests version
independent, refresh lockfile as needed, and replace first-publication instructions with
the current account setup. Re-run the release checks and commit the version preparation.
The release tag is `npm-v0.1.1`; Obsidian and private workspace versions are independent.

Local `npm whoami` returned `kywk`; `npm trust list` returned 403 and a notice about
bypass-2FA token restrictions. Existing publishers cannot be inferred from this response.
Manual web login/2FA and verification of each package's publisher remain necessary.
Use repository `kywk/story-map`, workflow `release-npm.yml`, blank environment, and direct
publish permission. After account configuration is confirmed, push the release commit
and tag, observe the workflow, and verify all three `latest` versions.

The original plan and validation report remain historical records for 0.1.0 preparation;
they do not supersede the registry findings recorded here.
