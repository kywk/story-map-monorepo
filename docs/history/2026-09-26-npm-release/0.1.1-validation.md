# 0.1.1 preparation validation

2026-09-26: the three public libraries now use `0.1.1`. Private workspace and Obsidian
versions are unchanged. Lockfile refresh made no changes because internal workspace
dependencies remain links.

Passed: frozen install, typecheck, all 92 tests (86 package + 6 publishing guards),
full build, and isolated tarball installation/SSR/Remark/CSS/declaration checks.
Local verification used Node 26.8.2 and pnpm 12.6.0; GitHub selects Node 24.

Verified tarballs: `/tmp/story-map-packed-J6CPnQ/` (temporary local artifacts).
No npm publication or release tag push occurred during these checks. Current registry
versions remain `0.1.0`. Account-side Trusted Publisher confirmation and actual GitHub
OIDC execution are still pending, as are the manual host smoke tests previously recorded.
